#!/bin/bash

# Build docker image
git clone https://github.com/mobula/dokku-pg-dockerfiles /tmp/dokku-pg-dockerfiles
docker build -q=true -t mobula/postgresql /tmp/dokku-pg-dockerfiles
rm -rf /tmp/dokku-pg-dockerfiles
